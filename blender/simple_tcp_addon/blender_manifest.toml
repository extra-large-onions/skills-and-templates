schema_version = "1.0.0"

id = "simple_tcp_server"
version = "1.0.0"
name = "Simple TCP Server"
tagline = "Minimal TCP socket server for direct Python code execution"
maintainer = "local"
type = "add-on"
blender_version_min = "4.2.0"

license = ["SPDX:GPL-3.0-or-later"]
tags = ["Development"]

[permissions]
network = "Runs a local TCP socket server on localhost"
